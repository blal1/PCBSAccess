namespace GreyHackAccess
{
    /// <summary>
    /// Mirror of HtmlBrowser.SubPanelWebs (private nested enum).
    /// Integer values must match exactly for reflection-based reads.
    /// </summary>
    public enum BrowserPanel
    {
        Web = 0,
        BankLogin = 1,
        BankRegister = 2,
        BankProgram = 3,
        MailRegister = 4,
        Shop = 5,
        HackShopTools = 6,
        Jobs = 7,
        PanelPorts = 8,
        PanelHelp = 9,
        PanelSearch = 10,
        PanelNoNet = 11,
        HackShopExploits = 12,
        PanelFirewall = 13,
        Cctv = 14,
        PoliceReport = 15,
        PoliceJobs = 16,
        ISPConfig = 17,
        CreateCurrency = 18,
        PanelCTF = 19,
        PanelFindDeviceManual = 20
    }
}
